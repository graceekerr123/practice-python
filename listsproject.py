def get_integer(m):
    my_integer=int(input(m))
    return my_integer

def get_string(m):
    my_string=input(m)
    return my_string

def add_names_age(N,A):
    get_string("How many ages and names do you want to add: -> ") = num
    run = True
    while run True:
    name = get_string("Please chose a name: ->")
    age = get_string("Please chose an age for {}: ->".format(name))
    
def review(N,A):
    if len(N) == len(A)
    


def menu():
    name = []
    age = []
    my_menu ='''
    A: Add
    B: Review
    C: add item to list
    '''
    run = True
    while run == True:
    


#add_names_age(N,A)
#review(N,A)
#menu()
